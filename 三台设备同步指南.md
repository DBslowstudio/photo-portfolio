# 三台设备同步指南（Windows ×2 + Mac ×1）

> 项目新位置：`D:\Projects\photo-portfolio`
> 已从我的会话工作目录迁出（原路径带会话标识，有被清理风险）。
> 迁移后已验证：构建通过 10 页、工作区干净、Vercel 关联保留、47 个文件完整。

---

## 一、当前状态

| 项目 | 状态 |
| --- | --- |
| 本地 Git 仓库 | ✅ 已建立，分支 `main` |
| 首次提交 | ✅ `604173d`，47 文件 / 2668 行 |
| 工作区 | ✅ 干净，无未提交改动 |
| 凭据安全 | ✅ `.env` 不存在（只有模板），已被 `.gitignore` 排除，**仓库内无任何凭据** |
| 跨平台行尾 | ✅ 已加 `.gitattributes`，统一 LF |
| Git 程序 | ✅ 本机 v2.55.0.3（`C:\Program Files\Git`） |
| Vercel 关联 | ✅ 保留，`projectName = photo-portfolio` |
| **GitHub 远程仓库** | ❌ **还没建，代码仍未离开本机** |

⚠️ **现在仍然只有一份副本。** 本地仓库只解决了「可回滚」，没解决「换电脑能继续」。必须完成第二节的推送才算真正安全。

---

## 二、这台电脑：推送到 GitHub（只剩这一步）

### 第 1 步：在 GitHub 建一个空仓库（浏览器操作）

1. 打开 `https://github.com/new`
2. Repository name 填 `photo-portfolio`（或你喜欢的名字）
3. **选 Private**（私人仓库，照片和代码不公开）
4. ⚠️ **不要勾选** "Add a README file"、".gitignore"、"license" 任何一项——必须建成完全空的仓库，否则首次推送会因历史冲突失败
5. 点 Create repository，然后**复制页面上给出的仓库地址**（形如 `https://github.com/你的用户名/photo-portfolio.git`）

### 第 2 步：关联并推送（PowerShell）

```powershell
cd D:\Projects\photo-portfolio
git remote add origin 把上面复制的地址粘贴到这里
git push -u origin main
```

首次推送会弹出 GitHub 登录窗口（Git Credential Manager），**用你的账号登录并授权**。这个授权只能你本人完成，我不会也不该经手你的密码或令牌。

### 第 3 步：改提交身份（建议）

我给这个仓库设的是占位身份 `Daobo <Daobo@users.noreply.github.com>`，且**只设在仓库级，没动你的全局 Git 配置**。如果你想让提交显示成自己的 GitHub 头像：

```powershell
cd D:\Projects\photo-portfolio
git config user.name "你的GitHub用户名"
git config user.email "你的GitHub注册邮箱或noreply邮箱"
```

> 你的 GitHub noreply 邮箱可在 `https://github.com/settings/emails` 查到，形如 `12345+用户名@users.noreply.github.com`。用它比用真实邮箱更能保护隐私。

---

## 三、另外两台设备：如何接着工作

### Windows 电脑（另一台）

需要先装 Git（这台 Mac 自带，Windows 不自带）：

```powershell
winget install --id Git.Git -e --accept-package-agreements
```

装完**关掉并重开终端**（否则 PATH 不生效，会报「无法将 git 项识别为 cmdlet」——你昨天遇到的 npm 报错就是同一个原因）。

然后克隆项目：

```powershell
cd D:\Projects
git clone https://github.com/你的用户名/photo-portfolio.git
cd photo-portfolio
npm install
```

### Mac

macOS 自带 Git，第一次运行 `git` 会提示安装命令行工具，点确认即可。

```bash
cd ~/Projects          # 或你习惯的开发目录
git clone https://github.com/你的用户名/photo-portfolio.git
cd photo-portfolio
npm install
```

> Mac 还需要 Node.js（如果没有）：`brew install node`，或去 `https://nodejs.org` 下载 LTS 版本。

### 三台设备的日常节奏

**开始工作前，先拉取最新：**

```bash
git pull
```

**改完代码后，提交并推送：**

```bash
git add .
git commit -m "说明这次改了什么"
git push
```

**核心纪律：每次离开一台电脑前先 `push`，每次坐到另一台前先 `pull`。** 只要守住这一条，就不会出现「昨天改的东西找不到」或「两台机器互相覆盖」。

⚠️ 如果两台设备改了同一个文件又没及时推送，会产生合并冲突。冲突不可怕，Git 会标出冲突位置让你选保留哪边，但**预防远比处理容易**——所以别囤着不推。

---

## 四、`npm install` 在新设备上要多久

`node_modules` **不入仓库**（693 MB / 78429 个文件，同步它既慢又容易损坏），每台机器自己装。

- 首次 `npm install`：需要下载，几分钟
- 之后：`package-lock.json` 已入库，保证三台机器装到**完全相同的依赖版本**，不会出现「这台能跑那台报错」

如果新机器上装完构建报错，先确认 Node 版本：本项目需要 **Node ≥ 22.12**（当前这台是 v24.20.0）。

---

## 五、⚠️ 凭据不要在设备间用 Git 传

`.env` 文件（TinaCMS 的 Client ID 和 Token）**被 `.gitignore` 排除，永远不会进仓库**——这是故意的，不是遗漏。

三台设备都需要用后台时，**每台单独建自己的 `.env`**：

```bash
# Windows PowerShell
cd D:\Projects\photo-portfolio
Copy-Item .env.example .env
notepad .env

# Mac / Linux
cd ~/Projects/photo-portfolio
cp .env.example .env
open -e .env
```

填入你从 TinaCloud 拿到的两个值。**绝对不要**为了省事把 `.env` 强制加进仓库（`git add -f .env`）——那等于把后台权限公开到 GitHub 上，即使是 Private 仓库，一旦泄露到日志或截图里就收不回来。

---

## 六、可选：把 Vercel 改成 GitHub 自动部署

推送成功后，你可以升级部署方式：

1. Vercel 控制台 → **Add New → Project** → 选择刚推送的 `photo-portfolio` 仓库
2. Framework 会自动识别为 Astro
3. Build Command 填 `npm run build`（启用 TinaCMS 后台则填 `npm run build:cms`）

之后**每次 `git push` 都会自动触发构建部署**，不用再手动跑 `vercel --prod`。

当前方式（CLI 直传）仍然可用，Vercel 关联已保留。两种都行，看你习惯。

⚠️ 如果改用 GitHub 自动部署，Vercel 云端是全新安装环境，`package.json` 里 `allowScripts` 放行 `better-sqlite3` 那项**是必需的**（TinaCMS 依赖它，不放行会导致云端构建崩溃）。这项我已经加好了。

---

## 七、这次做了什么、验证了什么

**做的操作：**

1. 扫描全盘确认项目只有单份副本、无任何备份
2. 项目从 `D:\Documents\qwen-agent\iy1XB1NC27\default\` 迁到 `D:\Projects\photo-portfolio`（同盘移动，秒级完成；迁前已确认目标无同名内容，迁后核验文件数 78514 与迁前一致）
3. 用 winget 安装 Git 2.55.0.3
4. 新建 `.gitattributes` 统一跨平台行尾（否则 Windows 的 CRLF 与 Mac 的 LF 会让每次跨机器同步都显示大量虚假改动）
5. `git init -b main` 建立仓库，设置仓库级提交身份（未动全局配置）
6. 暂存并完成首次提交 `604173d`

**安全核验（提交前逐项确认）：**

- `.env` / `.env.local` / `.env.production` **均不存在**，确认无真实凭据
- `.env.example` 逐行检查，**只有占位符**（`在这里粘贴你的_Client_ID`），无真实值
- 暂存区 47 个文件全为源码，**不含** `node_modules`、`dist`、`.vercel`、`public/admin`
- `git check-ignore` 逐项验证排除规则生效
- 扫描暂存内容，未发现私钥、GitHub token、TinaCloud 凭据等敏感模式

**迁移后核验：**

- `npm run build` 通过，10 个页面，EXIF 脚本正常执行（脚本用相对路径，未受搬迁影响）
- 构建后 `git status` 干净，证明产物被正确忽略
- Vercel 关联保留（`projectName = photo-portfolio`），明天可直接 `vercel --prod`
- 47 个受版本管理文件与提交数一致，关键配置文件无缺失

**未验证：** GitHub 推送本身（需要你的账号授权，无法代办），以及另两台设备上的克隆与构建（需要你在那些机器上操作）。

---

## 八、关于我之前那个失误

昨天我覆盖 `scripts/extract-exif.js` 时无法恢复，根本原因就是**没有版本历史**。现在有了：任何文件被误改或误删，都能用 `git checkout` 或 `git revert` 找回。

这也是为什么我把「建本地仓库」放在「推送到 GitHub」之前先做——即使你今晚不建 GitHub 仓库，本地版本历史也已经开始保护你的工作了。

---

## 九、明天出门前的检查清单

- [ ] GitHub 上建好空仓库（不勾任何初始化选项）
- [ ] `git remote add origin <地址>` + `git push -u origin main` 成功
- [ ] 浏览器打开仓库页面，确认 47 个文件都在
- [ ] （可选）改掉占位提交身份
- [ ] 确认 `.env` 从未被提交：`git log --all --full-history -- .env` 应无任何输出
- [ ] 如已配好 TinaCloud，在 Vercel 后台填 `TINA_CLIENT_ID` / `TINA_TOKEN` 环境变量
- [ ] 如需本轮改动上线：`vercel --prod`
