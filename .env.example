# TinaCMS 凭据模板
#
# 用法：复制本文件为 .env（.env 已被 .gitignore 排除，不会提交），
# 填入你从 https://app.tinacloud.com 项目设置里拿到的两个值。
#
# ⚠️ 这两个值等同于后台的访问权限：
#    - 不要提交进 Git 仓库
#    - 不要截图发到群里
#    - 不要粘贴给任何人（包括 AI 助手）
#    如果怀疑泄露，去 TinaCloud 后台重新生成即可。
#
# 没填这两个值时：/admin 后台无法登录，但网站本身照常构建和部署，
# 用 `npm run build:site` 即可（不含 tinacms build）。

TINA_CLIENT_ID=在这里粘贴你的_Client_ID
TINA_TOKEN=在这里粘贴你的_Read-Only_或_Full_Access_Token

# 仅在本地开发且想区分凭据时才需要，一般留空即可
TINA_TOKEN_LOCAL=
