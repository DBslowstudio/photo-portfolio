# 摄影画廊网站 · Vercel 部署操作清单

> 代码部分已全部完成并通过构建验证，这份清单只剩**需要你自己操作的账号授权步骤**。
> 全程不启动本地服务器，所有构建进程跑完自动退出。

---

## 一、当前状态（已完成）

| 项目 | 状态 |
| --- | --- |
| 项目路径 | `D:\Documents\qwen-agent\iy1XB1NC27\default\photo-portfolio` |
| Astro 版本 | 7.3.1 |
| Tailwind 版本 | 4.3.3（`@tailwindcss/vite` 插件方式） |
| PhotoSwipe 版本 | 5.4.4（灯箱可用） |
| 构建结果 | ✅ 10 个页面全部生成 |
| 类型检查 | ✅ 0 errors / 0 warnings / 0 hints |
| 产物体积 | 0.63 MB（含 7 张占位图） |

已生成的页面：首页（Hero + 6 图网格）、6 个作品详情页（灯箱 + EXIF 六项参数 + 上下张导航）、关于、联系、404。

---

## 二、你需要做的 4 步

### 第 1 步：安装 Vercel CLI（约 1 分钟）

打开 PowerShell，执行：

```powershell
npm install -g vercel
```

> 注意：文档里没提这一步，但 CLI 部署路线必须装。**不需要装 Git**——CLI 直传走的是本地文件上传，不依赖 Git 仓库。

### 第 2 步：登录 Vercel（需要你本人授权）

```powershell
cd D:\Documents\qwen-agent\iy1XB1NC27\default\photo-portfolio
vercel login
```

会自动打开浏览器要求登录。**这一步只能你自己做**：

- 没有 Vercel 账号就先注册，推荐用 GitHub 账号一键登录
- 登录后回到终端，会提示选择登录方式，选浏览器授权即可
- ⚠️ 不要把账号密码或验证码发给任何人（包括我）

### 第 3 步：部署上线（约 1 分钟）

```powershell
vercel --prod
```

首次运行会依次问你几个问题，按下面回答：

| 提问 | 回答 |
| --- | --- |
| Set up and deploy? | `Y` |
| Which scope? | 选你自己的账号 |
| Link to existing project? | `N` |
| What's your project's name? | 直接回车（用 `photo-portfolio`），或自己起一个 |
| In which directory is your code located? | 直接回车（`./`） |
| Override settings? | `N`（已配好 `vercel.json`，不要覆盖） |

部署完成后会输出一个网址，形如：

```
https://photo-portfolio-xxxx.vercel.app
```

打开这个网址，应该能看到暗色主题的摄影画廊首页。

### 第 4 步：回填真实域名（拿到网址后做）

部署成功后，把 Vercel 给你的网址填回配置文件。

**编辑** `astro.config.mjs` **第 8 行**，把：

```js
site: 'https://photo-portfolio.vercel.app',
```

改成你的实际网址：

```js
site: 'https://photo-portfolio-xxxx.vercel.app',
```

然后重新部署一次：

```powershell
npm run build
vercel --prod
```

> 这个 `site` 字段影响 SEO 的 canonical 链接。文档原文写的是 `https://你的域名.com`——那不是合法 URL，会让构建出的链接全部失效，所以我先放了一个合法占位值，**必须换成真的**。

---

## 三、以后想更新网站

改完代码或换完照片后，执行：

```powershell
cd D:\Documents\qwen-agent\iy1XB1NC27\default\photo-portfolio
vercel --prod
```

一条命令即可，不需要重新走登录流程。

---

## 四、换成你自己的照片

占位图是我用脚本生成的渐变图，**只为跑通全流程**，不是真实作品。替换方法：

1. 把你自己的照片重命名成下面这些文件名（名字不变就不用改代码）：

   ```
   public\hero-background.jpg          ← 首页全屏大图，建议 1920×1080
   public\photos\sunset-at-xiangshan.jpg
   public\photos\rainy-alley.jpg
   public\photos\misty-mountain.jpg
   public\photos\night-city.jpg
   public\photos\desert-dune.jpg
   public\photos\forest-path.jpg
   ```

   作品图建议统一 4:3 比例（如 1600×1200），网格排版最整齐。

2. **编辑** `src\data\photos.ts`，改每条记录的标题、日期、地点、描述和 EXIF 参数。

3. 如果想改照片数量：增删 `photos.ts` 里的记录，同时增删 `public\photos\` 下对应的图片文件即可，灯箱和上下张导航会自动适配。

4. ⚠️ **如果照片尺寸不是 1600×1200**，还要改 `src\pages\photos\[slug].astro` 第 24-25 行的 `IMAGE_WIDTH` / `IMAGE_HEIGHT`，否则灯箱打开时会因为预留比例不对而跳动。

5. 重新部署：`vercel --prod`

---

## 五、换成你自己的名字和联系方式

代码里有 4 处占位文案需要替换：

| 文件 | 位置 | 当前内容 |
| --- | --- | --- |
| `src\components\Header.astro` | 导航栏品牌名 | `你的名字` |
| `src\components\Footer.astro` | 版权行 | `你的名字` |
| `src\pages\index.astro` | Hero 大标题 | `你的名字` |
| `src\pages\contact.astro` | 联系方式列表 | `you@example.com` 等 4 项 |
| `src\pages\about.astro` | 自我介绍与器材 | 模板文案 |

`src\components\Footer.astro` 里的微博、Instagram 链接目前是 `href="#"`（占位），换成你自己的主页地址。

---

## 六、绑定自定义域名（可选，需要你自己操作）

如果要用自己的域名（文档提到年度约 50 元）：

1. Vercel 控制台 → 你的项目 → **Settings** → **Domains**
2. 输入你的域名，点 Add
3. Vercel 会给出 DNS 记录，到你的域名注册商后台添加：
   - 用 `www.xxx.com` 这类二级域名 → 加 **CNAME** 记录，指向 `cname.vercel-dns.com`
   - 用 `xxx.com` 裸域名 → 加 **A** 记录，指向 Vercel 给出的 IP
4. 等 DNS 生效（几分钟到几小时），Vercel 会自动签发 HTTPS 证书
5. 绑定后记得回到「第 4 步」，把 `astro.config.mjs` 里的 `site` 改成你的正式域名并重新部署

> ⚠️ 文档提到 `.vercel.app` 子域名在中国大陆访问可能不稳定。如果主要面向国内访客，绑定自定义域名会明显改善。

---

## 七、关于额度的说明

**这部分任务今天一次就做完了，不需要拆到 1-2 天。**

我这边完成的工作（补全重写代码 + 构建验证 + 类型检查 + 核验）消耗不大。真正跨步骤的只有你需要做的账号授权，那不受额度限制，你什么时候有空什么时候做即可。

另外提醒一个容易混淆的点：Vercel 免费 Hobby 套餐里有个「Vercel Sandbox 5 hours/month Active CPU」的额度，那是 **Vercel 云端沙箱的计算时长**，跟你在千问这边的使用额度完全是两回事。你这个纯静态站点（`output: 'static'`）不消耗它——静态页面由 CDN 直接返回，不触发任何服务端计算。

Vercel Hobby 免费套餐对这个站点的实际限制：

| 项目 | 免费额度 | 你的站点实际用量 |
| --- | --- | --- |
| 带宽 | 100 GB/月 | 产物 0.63 MB，个人作品集远够 |
| 项目数 | 200 个 | 1 个 |
| 每日部署 | 100 次 | 偶尔更新 |
| CLI 上传上限 | 100 MB / 15000 文件 | 0.63 MB，远低于限制 |

⚠️ 两个要注意的点：

- **Hobby 套餐禁止商业用途**。纯个人作品集没问题，如果将来接商业拍摄业务并用这个站点招揽客户，严格来说需要升级到 Pro（$20/月）。
- **照片体积会吃掉带宽**。占位图才 407 KB，但如果你换成 20 张 5 MB 的原图，单次页面加载就是上百 MB 流量。建议上传前把照片压到 300-800 KB 一张（长边 1600-2000px 足够）。

---

## 八、我在文档之外做的修改（与原规格的差异）

原文档有几处按字面执行会失败，我做了必要调整：

| # | 文档原文 | 问题 | 实际处理 |
| --- | --- | --- | --- |
| 1 | 文档在 5.11 节代码中途截断 | 没有 GitHub / Vercel / 域名任何部署步骤，也没有 about、contact、灯箱、EXIF 逻辑代码 | 按第一章承诺的功能全部补齐重写 |
| 2 | 5.11 用中文弯引号 `‘...’`、`—` 当分隔符、模板字符串丢反引号 | 照抄必然语法报错 | 重写为合法 Astro 代码 |
| 3 | `npm install @astrojs/tailwind tailwindcss` + `tailwind.config.mjs` + `@tailwind base;` | Tailwind v3 写法，`@astrojs/tailwind` 在 Astro 5+ 已废弃 | 按你选的方案改用 Tailwind v4 + `@tailwindcss/vite`，配色与 `.nav-link`/`.section-title` 等价迁移，不需要 `tailwind.config.mjs` |
| 4 | 装 `qrcode`、`exif-parser`、`@types/qrcode` | 文档给出的全部代码里一次都没用到（EXIF 是手写在 `photos.ts` 的静态数据） | 剔除，少装 3 个包 |
| 5 | `site: 'https://你的域名.com'` | 不是合法 URL，会让 canonical / sitemap 链接全部失效 | 先填合法占位值，部署后按第 4 步回填真实域名 |
| 6 | 只有 2 条作品数据 | 3 列网格排不满，看不出实际效果 | 保留原文 2 条不动，另补 4 条配套示例数据 |
| 7 | 未提及 | Header 用 `classList.add()` 动态挂类，Tailwind 扫不到字符串类名 | 在 `global.css` 用 `@source inline()` 显式登记，否则滚动时导航栏背景不变化 |

另外补了文档没有但部署必需的：`vercel.json`（框架识别 + 图片长缓存 + 安全响应头）、`404.astro`（自定义错误页）、`.gitignore` 的 `.vercel` 项、`engines` 明确为 `22.x`（Vercel 云端构建用范围值 `>=22.12.0` 解析不稳定，容易构建失败）。

---

## 九、本机环境备注

- Node.js `v22.14.0` ✅ 满足 Astro 7 要求（≥22.12.0）
- npm `10.9.2` ✅
- Git **未安装**——CLI 直传路线用不到，所以没装。如果将来想改用 GitHub 自动部署，再装也来得及。
- 安装依赖时有一条 `undici` 警告（要求 Node ≥22.19.0，本机 22.14.0）。这只是 npm 的引擎提示，**不影响构建和部署**，实测构建正常。如果想彻底消除，把 Node 升级到 22.19+ 即可，非必需。
