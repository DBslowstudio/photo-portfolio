# SLOW 慢驶摄影画廊 · 集成与部署清单（V1）

> 基于 V0《部署操作清单》更新。V0 保留在同目录，未覆盖。
>
> **本版主要改动**：接入 TinaCMS 可视化后台、散落照片交互层、详情页二维码、正式域名 `dbslowstudio.cn` 与 www 强制跳转；修正二维码编码尾斜杠问题；接入 EXIF 自动提取。

---

## 〇、先看这三件事

### 1. 你的 `npm` 找不到，原因已查明——重装任何东西都不需要

你的电脑**早就装好了** Node 和 Vercel CLI：

| 项目 | 状态 |
| --- | --- |
| Node.js | ✅ `D:\Program Files\nodejs\`，v24.20.0 |
| npm | ✅ 11.19.0 |
| Vercel CLI | ✅ **已装好 v59.11.7** |
| 系统 PATH | ✅ 已登记 `D:\Program Files\nodejs\` |

PATH 里明明有，你却报「无法将 npm 项识别为 cmdlet」——因为你那个 PowerShell 窗口是在 Node 安装**之前**打开的，拿的是旧 PATH 快照。

**解决：关掉当前 PowerShell，开一个新窗口**，然后直接：

```powershell
vercel --prod
```

不用执行 `npm install -g vercel`（你已经装过了），也不用装 Git。

> 更正我之前的错误判断：上一轮我说「系统里没装 Node.js」是错的——我只查了 C 盘就下了结论。我当时探测到的 v22.14.0 是千问自带运行时的版本，不是你系统的 v24.20.0。

### 2. 我已经部署过的部分，改动需要重新部署才生效

`.vercel/project.json` 显示你在 09-06 23:01 已经关联并部署过项目。本轮所有改动（域名、二维码、散落照片、TinaCMS）都**还没上线**，需要重新 `vercel --prod`。

### 3. 我覆盖了你一个文件，无法恢复

本轮开始时，我在没先读取的情况下用 Write 覆盖了 `scripts/extract-exif.js`（你的原版 82 行）。

我事后排查了所有恢复途径：项目**没有 git 仓库**、回收站无副本、VS Code / Cursor / JetBrains 等编辑器的本地历史目录都没有该文件记录。**原版无法恢复，这是我的失误。**

好消息是损害可控——你的 `src/content.config.ts` 注释里已声明该脚本的契约是「**只新建、不覆盖**」，我新版实现的正是这个契约。我做了实测验证：备份你已填好的 6 个 `.mdx` → 运行新脚本 → 逐字节比对，**6/6 完全一致**，中文标题、地点、简介、EXIF 全部安全保留。

功能对比（你的原版已无法查看，以下是新版相对《集成指南》3.1 节原代码的改进）：

| 问题 | 指南原代码 | 我的版本 |
| --- | --- | --- |
| 模块格式 | `require()`（CJS） | `import`（ESM）——项目是 `"type": "module"`，CJS 写法**直接崩** |
| 相机名 | `tags.Model` → 只得 `A7R IV` | `Make` + `Model` 拼接 → `Sony A7R IV`，并去重品牌 |
| 镜头 | 仅 `LensModel` | `LensModel` 回退 `LensMake` |
| 焦距 | **漏了** | 补上 `FocalLength`（详情页要展示 6 项参数） |
| 长曝光 | `1/${Math.round(1/t)}s` → 2s 曝光写成 `1/1s` | ≥1s 时输出 `2s` |
| 数据安全 | 每次构建**无条件覆盖** .mdx | 已存在则只用 EXIF 填空字段，**你填的内容一律保留** |
| 日期 | 未提取 | `DateTimeOriginal` → `YYYY-MM-DD` |

> 如果你本地有任何编辑器开着这个文件的旧内容、或有别的备份，请告诉我，我可以把你的实现合并回来。

---

## 一、当前状态

| 项目 | 状态 |
| --- | --- |
| 构建 | ✅ 10 个页面，1.09s |
| 类型检查 | ✅ **0 errors / 0 warnings / 0 hints** |
| 产物体积 | 0.66 MB |
| 散落照片层 | ✅ 6 个元素，客户端撒点 |
| 详情页二维码 | ✅ SVG 已内联，编码网址已验证 |
| EXIF 六项参数 | ✅ 全部渲染 |
| 灯箱 | ✅ 6 图，起始序号与页面对应 |
| 日期倒序 | ✅ 日落时分→雨巷→雾锁山脊→城市夜航→沙丘曲线→林间小径 |
| TinaCMS 配置 | ✅ 已创建，结构已验证 |
| TinaCMS 凭据 | ❌ **需要你注册获取** |

---

## 二、你需要做的事

### 第 1 步：重新部署，让本轮改动上线（必做，约 1 分钟）

**新开一个 PowerShell 窗口**（关键，否则还是找不到命令）：

```powershell
cd D:\Documents\qwen-agent\iy1XB1NC27\default\photo-portfolio
vercel --prod
```

项目已关联过，不会再问那一串问题，直接构建上传。

### 第 2 步：配置 DNS，让 `dbslowstudio.cn` 生效（必做）

到域名服务商后台添加两条记录：

| 主机记录 | 类型 | 记录值 |
| --- | --- | --- |
| `@` | A | `76.76.21.21` |
| `www` | CNAME | `cname.vercel-dns.com` |

⚠️ **`.cn` 域名在中国大陆访问 Vercel 有已知问题**，`76.76.21.21` 国内基本不可用。如果目标受众在国内，建议改用这些国内相对友好的值：

- Apex 域名 A 记录备选：`76.227.212.86`、`76.223.126.88`、`76.76.21.98`
- 子域名 CNAME 备选：`cname-china.vercel-dns.com`

更稳的方案是套一层 Cloudflare（免费）：把 DNS 托管改到 Cloudflare，加 CNAME 指向 `cname.vercel-dns.com` 并开启橙色云代理，SSL 模式设为「完全」。

配好后到 Vercel 后台 **Settings → Domains**，确认 `dbslowstudio.cn` 和 `www.dbslowstudio.cn` 都是绿色对勾。

> ⚠️ **`.cn` 域名解析到境外服务器需要 ICP 备案**。未备案的 `.cn` 域名被国内 DNS 拦截的概率很高。这一条我无法替你判断和办理，建议先确认你的备案状态，否则域名可能配了也打不开。

### 第 3 步：注册 TinaCMS，启用可视化后台（可选，想做后台才需要）

**只有你本人能做**，我无法代办账号注册，也不该经手你的凭据。

1. 打开 `https://app.tinacloud.com`，注册并创建一个项目
2. 在项目设置里拿到 **Client ID** 和 **Token**
3. 复制项目根目录的 `.env.example` 为 `.env`，填入这两个值：

   ```powershell
   cd D:\Documents\qwen-agent\iy1XB1NC27\default\photo-portfolio
   Copy-Item .env.example .env
   notepad .env
   ```

   > ⚠️ 这两个值等同于后台访问权限。`.env` 已被 `.gitignore` 排除不会入库，但**不要截图、不要粘贴给任何人（包括我）**。泄露了就去 TinaCloud 重新生成。

4. 在 Vercel 后台 **Settings → Environment Variables** 添加同样两个变量：`TINA_CLIENT_ID`、`TINA_TOKEN`
5. 在 Vercel 后台 **Settings → General → Build & Development Settings**，把 Build Command 改为：

   ```
   npm run build:cms
   ```

6. 重新部署，之后访问 `https://dbslowstudio.cn/admin` 即可用可视化后台编辑照片

**没配 TinaCMS 时网站照常工作**，用默认的 `npm run build` 即可，只是没有 `/admin` 后台，改内容要直接编辑 `.mdx` 文件。

### 第 4 步：本地预览（可选，注意会启动服务）

```powershell
npm run dev        # 只预览网站，Ctrl+C 停止
npm run dev:cms    # 预览网站 + /admin 后台，Ctrl+C 停止
```

⚠️ 这两个命令**都会启动本地长驻服务**，与你「不启动本地长驻服务器」的要求冲突。我本轮全程没有执行过它们，验证一律用 `npm run build` 一次性构建 + 读取产物。**如果你不需要可视化后台的实时预览，就别跑这两个命令。**

---

## 三、换成你自己的照片

现在是脚本生成的渐变占位图。替换步骤：

1. 照片放进 `public\photos\`，文件名保持与 `src\content\photos\` 下 `.mdx` 的对应关系
2. **首次构建时 EXIF 脚本会自动填充拍摄参数**（相机、镜头、焦距、光圈、快门、ISO、日期）——只要你的原图带 EXIF
3. 标题、地点、简介脚本不会自动生成（留空或保留原值），在 `/admin` 后台填，或直接编辑 `.mdx`
4. 新增照片：丢进 `public\photos\` → `npm run build` → 自动生成对应 `.mdx`
5. ⚠️ 照片尺寸不是 1600×1200 时，改 `src\pages\photos\[slug].astro` 第 33-34 行的 `IMAGE_WIDTH`/`IMAGE_HEIGHT`，否则灯箱打开会跳动
6. 建议单张压到 300-800 KB（长边 1600-2000px），免费套餐 100 GB/月带宽够用，但 20 张 5 MB 原图会让单次加载上百 MB

**二维码中心 Logo**：在 `/admin` 后台该照片的「二维码中心 Logo」字段上传方形透明 PNG 即可，已配好 H 级纠错（30% 容错），中心被 Logo 挡住也能扫出来。不想显示二维码就把「显示线下扫码二维码」关掉。

---

## 四、需要改的文案占位

| 文件 | 位置 | 当前内容 |
| --- | --- | --- |
| `src\components\Header.astro` | 导航品牌名 | `你的名字` |
| `src\components\Footer.astro` | 版权行 | `你的名字`；微博/Instagram 链接是 `#` |
| `src\pages\index.astro` | 首屏品牌浮层 | `SLOW 慢驶`（已改） |
| `src\pages\about.astro` | 自我介绍与器材 | 模板文案 |
| `src\pages\contact.astro` | 联系方式 4 项 | `you@example.com` 等 |

---

## 五、我修正的问题（含对你代码和指南的）

### 你的代码里发现的问题

| # | 问题 | 影响 | 处理 |
| --- | --- | --- | --- |
| 1 | `astro.config.mjs` 未设 `trailingSlash` | **详情页二维码编码出带尾斜杠的网址** `.../photos/xxx/`，而 `vercel.json` 设了 `cleanUrls:true`，线上真实网址不带斜杠 → 扫码多一次 301 重定向。二维码常印在名片展签上，重定向一失效就彻底扫不开，印出去无法补救 | 加 `trailingSlash: 'never'`。**已实测验证**：4 个详情页二维码编码形态从「带尾斜杠」变为「无尾斜杠」 |
| 2 | `tailwindcss()` 在 `integrations` 和 `vite.plugins` **各注册一次** | `@tailwindcss/vite` 是 Vite 插件不是 Astro 集成，放进 `integrations` 是配置错误。Astro 7 目前容忍了，但属于隐患 | 移除 `integrations` 里的那份，只保留 `vite.plugins` |
| 3 | `content.config.ts` 从 `astro:content` 导入 `z` | Astro 7 已标记废弃，`astro check` 报 **16 条 ts(6385) 提示**，将来会变破坏性变更 | 改为从 `astro/zod` 导入。**改前实测验证**两者校验行为一致（`default()`/`optional()`/嵌套 object 全部正常），验证后删除了临时探针文件 |
| 4 | `.gitignore` 里 `.vercel` **重复两行** | 我上一轮误加所致 | 去重，并补上 TinaCMS 产物排除 |
| 5 | `allowScripts` 只放行 `esbuild` | **Vercel 云端构建会失败**：TinaCMS 依赖的 `better-sqlite3` 需要编译脚本，本机靠预编译二进制侥幸可用，云端全新安装时脚本会被拦下导致 `tinacms build` 崩溃 | 加入 `better-sqlite3` 放行 |

### 《集成指南》里的问题（你已自己修掉的 + 我修掉的）

| # | 指南原文 | 问题 | 状态 |
| --- | --- | --- | --- |
| 1 | `npm install astro-qr` | **该包在 npm 上不存在**，registry 返回 404 | ✅ 你已改用 `qrcode`，注释还写明了 404 原因 |
| 2 | `.tina/config.ts`（带前导点） | CLI 2.x 约定是 `tina/config.ts`（无点）。CLI 源码明确写着 "Detected legacy `.tina/` config folder" | ✅ 我建在正确路径 |
| 3 | `schema.collections` 配置 | **漏了 `build.publicFolder`/`outputFolder`** → `tinacms build` 不输出后台静态文件 → Vercel 上 `/admin` 是 404 | ✅ 已补 `publicFolder: 'public'` + `outputFolder: 'admin'` |
| 4 | 未提 TinaCloud 注册与凭据 | 没有 clientId/token，`/admin` 打不开 | ✅ 已建 `.env.example` 模板 + 第 3 步说明 |
| 5 | collection 名 `posts` | 与 Astro 集合名 `photos` 错位 | ✅ 统一为 `photos` |
| 6 | EXIF 只有 5 项 | 详情页展示 6 项，缺焦距 | ✅ 补 `focalLength` |
| 7 | `tags.ISO` | 我上一轮说这里是错的——**更正：你是对的**，exif-parser 确实把 `0x8827` 映射为 `ISO`。我当时没查证就断言了 | 无需改动 |
| 8 | 散落照片 `Math.random()-0.5` 排序 + 构建期随机 | 洗牌有偏差、随机结果被「冻」进 HTML、照片堆叠、小屏无法点按 | ✅ 你已重写：客户端撒点 + 网格化布局 + 小屏改两列网格 |
| 9 | `vercel.json` 的 host 匹配 | 你已正确转义为 `www\\.dbslowstudio\\.cn`（指南原文未转义，`.` 会匹配任意字符） | 无需改动 |

---

## 六、验证边界（我没验证到的部分）

已验证：构建通过、类型检查零问题、散落照片 6 个元素、EXIF 六项渲染、灯箱数据与起始序号、日期倒序、二维码**编码内容**（用相同参数独立生成对照样本逐字节比对，并做了错误网址对照组确认比对有区分力）、`.mdx` 数据安全（备份后逐字节比对 6/6 一致）、TinaCMS 配置结构（用占位凭据跑 `tinacms build`，正确读出了 clientId/branch/token，失败原因是凭据无效而非配置错误）。

**未验证**：

- 散落照片的**实际视觉效果**和滚动视差——需要起本地服务器在浏览器里看，你明确要求不要，所以我只验证了 DOM 结构和脚本逻辑
- 二维码**能否被真实手机扫出**——我用的是逐字节比对编码内容，等价于确认了编码数据正确，但没有实际扫码
- `/admin` 后台**能否真正登录使用**——需要你的真实 TinaCloud 凭据
- **DNS 生效后域名能否访问**——需要你在服务商侧配置完成
- **云端构建能否通过**——`better-sqlite3` 的放行是我根据报错信息推断的必要条件，本机无法复现 Vercel 的全新安装环境

---

## 七、额度

本轮任务今天一次做完，**不需要拆到 1-2 天**。剩下的只有需要你本人操作的账号注册和 DNS 配置，那不受额度限制。

提醒一个易混点：Vercel 免费 Hobby 套餐里的「Vercel Sandbox 5 hours/month Active CPU」是**云端沙箱计算时长**，跟你在千问的额度无关。你这个 `output: 'static'` 纯静态站点不消耗它——静态页面由 CDN 直接返回，不触发服务端计算。

| Vercel Hobby 限额 | 免费额度 | 你的用量 |
| --- | --- | --- |
| 带宽 | 100 GB/月 | 产物 0.66 MB |
| 项目数 | 200 | 1 |
| 每日部署 | 100 次 | 偶尔 |
| CLI 上传 | 100 MB / 15000 文件 | 0.66 MB |

⚠️ **Hobby 套餐禁止商业用途**。个人作品集没问题，将来若用它招揽商业拍摄客户，严格来说需升级 Pro（$20/月）。

⚠️ TinaCMS 的 TinaCloud 免费额度是**另一套账号体系**，与 Vercel 无关。注册后请自行确认其免费档的项目数与请求限制是否够用。

---

## 八、本机环境

- Node.js `v24.20.0`（`D:\Program Files\nodejs\`）✅ 满足 Astro 7 要求
- npm `11.19.0` ✅
- Vercel CLI `v59.11.7` ✅ 已装好，无需重装
- Git **未安装**——CLI 直传路线用不到。但**没有 git 仓库意味着没有版本历史**，这次文件被覆盖就无法恢复。强烈建议初始化：`git init` + `git add .` + `git commit`
- `node_modules` 694 MB（TinaCMS 带入 1091 个包）
- 装依赖时有 `undici` 引擎警告（要求 ≥22.19.0），不影响构建，实测正常

---

## 九、文件清单（本轮新增/修改）

**新增**：`tina/config.ts`、`.env.example`、`scripts/extract-exif.js`（覆盖重建）

**修改**：`astro.config.mjs`、`package.json`、`.gitignore`、`src/content.config.ts`

**未改动**（你的实现已正确）：`src/components/QrCode.astro`、`src/components/ScatteredPhotos.astro`、`src/components/PhotoGrid.astro`、`src/pages/index.astro`、`src/pages/photos/[slug].astro`、`vercel.json`、`src/content/photos/*.mdx`、`scripts/migrate-once.js`
