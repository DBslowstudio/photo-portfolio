# SLOW 慢驶 · 灾备接续手册

> 备份日期：2026-09-25 · 对应提交 `a487ce77`（main 与 GitHub origin 完全同步）
> 本文件与同文件夹的备份件放在一起：拷走这个文件夹 = 拷走整个网站。

## 这个文件夹里有什么

| 文件 | 是什么 |
| --- | --- |
| `photo-portfolio-SOURCE.zip` | **当前网站全量快照**（288 张照片 + 全部代码内容），解压即可用 |
| `photo-portfolio-HISTORY.bundle` | Git 完整历史 bundle，含所有提交，可从任意旧提交恢复 |
| `rename-mapping.csv` | 照片改名对照：`p-001` ↔ 原中文名 `20260912整理_1` |
| `TinaCloud-env-敏感勿外传.txt` | TinaCMS 凭据（.env 副本）。丢失=进不了后台；外泄=别人能改你网站。**只放本机，绝不上传/发送** |
| `三台设备同步指南.md` / `集成与部署清单.md` / `TinaCMS启用指引.md` | 历史操作文档 |

## 线上现状一句话

https://dbslowstudio.cn：首页散落照片、碎片 `/glimpses`（288 张，时间轴+筛选+搜索）、专题 `/projects`（图录列表+暗室）、关于/联系/404、后台 `/admin`。
**日常维护：后台编辑或换照片 → git push → 自动部署上线。本地不需要跑任何服务。**

## 场景 A：网站没了，重建一模一样的（约 15 分钟）

前提：一台能上网的电脑 + Node.js LTS（nodejs.org 装好）。

```powershell
# 1) 解压源码快照
Expand-Archive photo-portfolio-SOURCE.zip -DestinationPath D:\Projects\photo-portfolio
cd D:\Projects\photo-portfolio

# 2) 装依赖，放凭据（把本文件夹的 env 文件改名复制为 .env）
npm install
Copy-Item "TinaCloud-env-敏感勿外传.txt" ".env"

# 3) 本地验证：一次性构建，跑完自动退出
npm run build:local
#    看到 295 page(s) built = 成功

# 4) 恢复 GitHub：在 github.com 重建 photo-portfolio 空仓库后
git push -u origin main --force
```

第 4 步后，在 Vercel 重新连接该 Git 仓库（Framework 自动识别 Astro，Build Command 填
`npm run build`），并在 Settings → Environment Variables 加 `TINA_CLIENT_ID`、`TINA_TOKEN`
（值就在 env 文件里）。之后每次 push 自动部署，回到现状。

**若本地连 .git 都没了**：`git clone photo-portfolio-HISTORY.bundle r && cd r && git checkout main`
即拿回完整历史再 push；或 `git init` + 全量 commit（丢历史、保内容）。
**GitHub 本身就是第一备份**：只要远端仓库在，`git clone` 它 + 放回 `.env` 就是完整恢复——
本文件夹的 zip/bundle 是防 GitHub 也没了的第二重保险。

## 场景 B：后台进不去 / 保存报错

| 症状 | 处置 |
| --- | --- |
| Schema Mismatch | 改过字段没重生成锁：项目里跑 `npx tinacms build`（需 .env），提交生成的 `tina/tina-lock.json`，等部署 |
| `Invalid path ... invalid characters` | 照片文件名有中文：改 `p-xxx` 式英文名（校验只认 `a-zA-Z0-9-_. /`） |
| 保存无反应 | `TINA_CLIENT_ID`/`TINA_TOKEN` 失效，去 app.tina.io 重新生成并同步更新 .env 与 Vercel 环境变量 |

## 场景 C：照片丢了

先查 `rename-mapping.csv`——多半没丢，是改名了。真删了则：
`git log --diff-filter=D --name-only -- public/photos` 找到删除提交，
`git show <提交>:public/photos/<文件名> > <文件名>` 捞回。

## 关键约定（新人必读，防踩已踩过的坑）

- 文件名只用 `字母数字 - _ . /`；**中文只放 title 字段，绝不放文件名**
- 新照片：丢进 `public/photos/`（英文名）→ push；构建自动提取 EXIF 生成条目
- 专题清单：一行一个 `p-001` 式名字，顺序即展线；**填错会被静默跳过**（看详情页进度计数自查）
- 本地验证用 `npm run build:local`；`npm run build` 含联网的 tinacms build，那是云端的事
- **push 前先 `git pull`**——后台保存会随时往 main 写提交，直接 push 会被拒
- `public/admin/`、`tina/__generated__/` 是构建产物，别手改
- 三台设备同步步骤见 `三台设备同步指南.md`（核心：离手前 push，上手后 pull）

## 进度快照（2026-09-25）

✅ 1.0 全站（首页/关于/联系/详情/灯箱/EXIF）→ ✅ 2.0 碎片档案库（时间轴/筛选/搜索/移动端抽屉）→ ✅ 2.0 专题轨道（图录列表/暗室序言帧/键鼠滑动箭头四操作/中英 blurb）→ ✅ TinaCMS 全链路自动部署
⏳ 待办（非紧急）：AI 批量打标（`scripts/ai_tag.py` 就绪，卡 Ollama 安装包手动下载）；WebP 缩略图（288 张规模已值得做）；关于页落款等细节由用户自维护
